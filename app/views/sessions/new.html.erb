<% provide(:title, "login") %>

<div class="row">
  <div class="col-md-7 col-md-offset-2">
    <h1 class="login-top">ログイン</h1>
    
    <%= form_with(scope: :session, url: login_path, local: true) do |f| %>
    
      <%= f.label :email, class: "label-login" %>
      <%= f.email_field :email, class: "form-control" %>
      
      <%= f.label :passwors, class: "label-login" %>
      <%= f.password_field :passwors, class: "form-control" %>
      
      <%= f.check_box :remember_me, class: "form-check-input" %>
      <%= f.label :remember_me, class: "form-check-label" %>
      <span>ログイン情報を記憶する</span>
      
      <%= f.submit "Log in", class: "btn btn-primary btn-block btn-login", id: "login_btn" %>
      <p class="login_link">アカウント作成は<%= link_to "こちら", "" %>から</p>
    <% end %>  
  </div>
</div>