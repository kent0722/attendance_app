<% provide(:title, "new") %>
<% provide(:class_text, 'signup') %>
<% provide(:button_text, '作成') %>

<div class="row">
  <div class="col-md-6 col-md-offset-2">
    <h1>アカウント作成</h1>
    <%= render 'form' %>
  </div>
</div>